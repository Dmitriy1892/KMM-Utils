import SwiftUI
import KmmUtils

struct ContentView: View {
    let greet = "text"
    
	var body: some View {
		Text(greet)
	}
}

struct ContentView_Previews: PreviewProvider {
	static var previews: some View {
		ContentView()
	}
}
